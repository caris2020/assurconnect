# Configuration de la base de données
spring.datasource.url=jdbc:postgresql://localhost:5432/assurance
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuration du serveur
# Utilise la variable d'environnement PORT si présente (Fly.io), sinon 8080 par défaut
server.port=${PORT:8080}

# Configuration des logs
logging.level.com.assurance=DEBUG
logging.level.org.springframework.web=DEBUG

# Configuration email (définir via variables d'environnement en prod)
# spring.mail.host=
# spring.mail.port=
# spring.mail.username=
# spring.mail.password=
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=

# Configuration email (Outlook/Hotmail) - Décommentez pour utiliser Outlook
# spring.mail.host=smtp-mail.outlook.com
# spring.mail.port=587
# spring.mail.username=VOTRE_EMAIL@outlook.com
# spring.mail.password=VOTRE_MOT_DE_PASSE
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=smtp-mail.outlook.com

# Configuration des fichiers
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configuration CORS (gérée par WebConfig.java)
# Pas besoin de configuration ici car gérée dans WebConfig.java


